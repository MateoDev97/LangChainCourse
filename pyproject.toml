[project]
name = "section2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MateoDev97", email = "mateo972000@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "black>=25.11.0",
    "isort>=6.1.0",
    "langchain-google-genai>=2.1.9,<3",
    "langchain-openai>=0.3.35",
    "python-dotenv>=1.2.1",
]

[project.scripts]
section2 = "section2:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
